package myPackage;

public class Ages {
    public static void main(String[] args) {

        //int[] tabs = new Tab[3];
        int[] tabs = {12, 45, 23, 1};

        //    for (int i = 0; i < 5; i++) {

        //    for (tabs[i] = 0; tabs[i] < 5; i++) {
        int sum = tabs[1] + tabs[2] + tabs[0] + tabs[3];
        System.out.println("Suma = " + sum);

        double sr = sum / 4;
        System.out.println("średnia wynosi " + sr);


        //  for (int i = 0; i < 3; i++) {
        //    int suma = 0;
        //  suma += tabs[i];
        // System.out.println("Suma wyrazów tablicy wynosi: " + sum);

        int BigesAges = 0;
        for (int tab : tabs) {
        if (BigesAges < tabs()) {
            BigesAges = tabs();
        }
    }
    private static int tabs() {
    }
    for (int tab : tabs);
    if (BigesAges == tabs())

}



